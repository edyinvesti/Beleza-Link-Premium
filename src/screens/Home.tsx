import { useNavigate } from "react-router-dom"; export default function Home() { const navigate = useNavigate(); return ( <div className="min-h-screen bg-[#050505] text-white flex flex-col items-center justify-center p-6 text-center"> <h1 className="text-6xl md:text-8xl font-black italic tracking-tighter uppercase mb-10">BELEZA <span className="text-amber-500">LINK</span></h1> <button onClick={() => navigate("/painel")} className="bg-white text-black px-12 py-5 rounded-2xl font-black uppercase hover:bg-amber-500 transition-all shadow-2xl active:scale-95">Entrar no Sistema</button> </div> ); }